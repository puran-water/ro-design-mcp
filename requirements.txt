# Core dependencies
fastmcp>=0.1.0
numpy>=1.24.0
papermill>=2.5.0
nbformat>=5.0.0
pydantic>=2.0.0

# WaterTAP dependencies (required for Tool 2: simulate_ro_system)
watertap>=0.11.0
pyomo>=6.7.0
idaes-pse>=2.2.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Testing dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-asyncio>=1.0.0
pytest-mock>=3.10.0

# Configuration dependencies
pyyaml>=6.0

# Optional: GA optimization (Phase 4)
# deap>=1.3.0,<1.4.0

# Optional: Thermodynamic calculations for scaling prediction
# reaktoro>=2.0.0  # Note: Requires conda installation, not available via pip
# Install with: conda install -c conda-forge reaktoro