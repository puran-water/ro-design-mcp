# RO Design MCP Server Environment Variables
# Copy this file to .env and update with your local configuration

# IDAES Configuration (Required on Windows)
# On Windows, IDAES requires LOCALAPPDATA to be set
# The server will auto-set this if not provided, but you can override here
# LOCALAPPDATA=C:\Users\YourUsername\AppData\Local

# Project Configuration (Optional)
# The server auto-detects these, but you can override if needed
# PROJECT_ROOT=/path/to/ro-design-mcp

# Virtual Environment Path (for installation scripts)
# Update this to match your virtual environment location
VENV_PATH=../venv312

# Jupyter Configuration (Optional)
# Prevents deprecation warnings
JUPYTER_PLATFORM_DIRS=1

# Solver Configuration (Optional)
# Path to IPOPT executable if not in system PATH
# PYOMO_IPOPT_PATH=/path/to/ipopt

# Logging Configuration (Optional)
# Set logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL=INFO

# Modal Deployment (Optional - for cloud deployment)
# MODAL_TOKEN_ID=your_modal_token_id
# MODAL_TOKEN_SECRET=your_modal_token_secret

# Development Settings (Optional)
# Enable debug mode for detailed error messages
# DEBUG=false

# Timeout Settings (Optional)
# Notebook execution timeout in seconds (default: 1800 = 30 minutes)
# NOTEBOOK_TIMEOUT=1800

# Parallel Execution (Optional)
# Number of parallel notebook executions allowed
# MAX_PARALLEL_NOTEBOOKS=4